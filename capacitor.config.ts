import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'consulta_cep',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
